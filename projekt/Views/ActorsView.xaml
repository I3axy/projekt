<UserControl x:Class="projekt.Views.ActorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:projekt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Actors Management" 
                   FontSize="24" FontWeight="Bold" 
                   Margin="10" Foreground="#2d3748"/>
        
        <!-- Search and Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Width="200" Height="30" 
                     VerticalContentAlignment="Center"
                     Margin="0,0,10,0"/>
            <Button Content="Search" Command="{Binding SearchCommand}"
                    Background="#3182ce" Foreground="White" 
                    Padding="10,5" BorderThickness="0" Margin="0,0,10,0"/>
            <Button Content="Add Actor" Command="{Binding AddCommand}"
                    Background="#38a169" Foreground="White" 
                    Padding="10,5" BorderThickness="0" Margin="0,0,10,0"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}"
                    Background="#e53e3e" Foreground="White" 
                    Padding="10,5" BorderThickness="0"/>
        </StackPanel>
        
        <!-- Actors Grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Actors}" 
                  SelectedItem="{Binding SelectedActor}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#f8f9fa"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="2*"/>
                <DataGridTextColumn Header="Birth Date" Binding="{Binding BirthDate, StringFormat=yyyy-MM-dd}" Width="*"/>
                <DataGridTextColumn Header="Nationality" Binding="{Binding Nationality}" Width="*"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
